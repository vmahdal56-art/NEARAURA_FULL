const functions = require("firebase-functions");
const admin = require("firebase-admin");
const { Faker, en } = require("@faker-js/faker");
const { Storage } = require("@google-cloud/storage");
// ... (rest of your imports from the file)
