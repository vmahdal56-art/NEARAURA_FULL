<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:padding="32dp"
    tools:context=".ui.LocationPermissionActivity">

    <!-- Icon/Illustration -->
    <ImageView
        android:id="@+id/image_location"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="64dp"
        android:src="@drawable/ic_location_on"
        app:tint="@color/colorPrimary"
        android:contentDescription="Location Required"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 1. Title -->
    <TextView
        android:id="@+id/title_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="Enable Location Services"
        android:textColor="@color/text_primary"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/image_location" />

    <!-- 2. Instruction Text -->
    <TextView
        android:id="@+id/instruction_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="NearAura is a location-based app. We need access to your location to find nearby users and connect you with your community."
        android:textColor="@color/text_secondary"
        android:textSize="18sp"
        android:textAlignment="center"
        android:lineSpacingExtra="4dp"
        app:layout_constraintTop_toBottomOf="@id/title_location" />

    <!-- 3. Permission Requirements List -->
    <LinearLayout
        android:id="@+id/permission_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toBottomOf="@id/instruction_location">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="• Discover users nearby (Fine Location)"
            android:drawableStart="@drawable/ic_check_circle"
            android:drawablePadding="8dp"
            android:gravity="center_vertical"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:layout_marginBottom="12dp"
            app:drawableTint="@color/colorPrimary"/>

    </LinearLayout>


    <!-- 4. Action Button (Continue) -->
    <Button
        android:id="@+id/button_grant_permission"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Grant Location Access"
        android:textSize="18sp"
        android:padding="14dp"
        android:backgroundTint="@color/colorPrimary"
        android:textColor="@android:color/white"
        app:cornerRadius="12dp"
        app:layout_constraintBottom_toTopOf="@id/text_later_disclaimer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 5. Later Disclaimer -->
    <TextView
        android:id="@+id/text_later_disclaimer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You can always change this in your phone settings later."
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:textAlignment="center"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>